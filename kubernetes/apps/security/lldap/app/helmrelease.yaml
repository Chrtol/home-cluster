# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app lldap
  namespace: security
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
      strategy: rollback
  uninstall:
    keepHistory: false
  # init-db initContainer + db.home.arpa/postgres pod label come from the cnpg
  # component (see ks.yaml). spec.values below deep-merges over this.
  # NOTE: the initContainer was renamed 01-init-db -> init-db to match the
  # component's key; lldap has no other initContainer, so the ordering prefix
  # was doing nothing.
  valuesFrom:
    - kind: ConfigMap
      name: lldap-cnpg-values
  values:
    controllers:
      lldap:
        replicas: 2
        strategy: RollingUpdate
        annotations:
          reloader.stakater.com/auto: 'true'
        containers:
          app:
            image:
              repository: ghcr.io/lldap/lldap
              tag: v0.6.3
            envFrom:
              - secretRef:
                  name: lldap-secret
            env:
              TZ: ${SECRET_TIMEZONE}
              LLDAP_HTTP_PORT: &port 80
              LLDAP_HTTP_URL: https://lldap.${SECRET_DOMAIN}
              LLDAP_LDAP_PORT: &ldapPort 389
              LLDAP_LDAP_BASE_DN: dc=${SECRET_DOMAIN_NAME},dc=com
              #Set the 2 vars below temporarily to true if  LLDAP_SERVER_KEY_SEED is changed
              LLDAP_FORCE_UPDATE_PRIVATE_KEY: "false"
              LLDAP_FORCE_LDAP_USER_PASS_RESET: "false"
            resources:
              requests:
                cpu: 15m
                memory: 105Mi
              limits:
                memory: 128M
    defaultPodOptions:
      priorityClassName: security-critical
    service:
      app:
        controller: lldap
        ports:
          http:
            port: *port
            primary: true
          ldap:
            enabled: true
            port: *ldapPort
    route:
      app:
        hostnames: ["lldap.${SECRET_DOMAIN}"]
        parentRefs:
          - name: internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: http
    persistence:
      data:
        enabled: true
        type: emptyDir
