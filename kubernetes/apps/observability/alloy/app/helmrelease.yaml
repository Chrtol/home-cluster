---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: alloy
    namespace: flux-system
  values:
    fullnameOverride: alloy

    controller:
      type: daemonset

    alloy:
      # Reads container logs via the Kubernetes API (loki.source.kubernetes),
      # so no /var/log/pods hostPath mounts are required.
      mounts:
        varlog: false
        dockercontainers: false
      configMap:
        content: |-
          // Discover every pod in the cluster.
          discovery.kubernetes "pods" {
            role = "pod"
          }

          // Port of Promtail's default kubernetes-pods scrape_config relabel rules,
          // preserving label parity (app, instance, component, node_name, namespace,
          // job=<namespace>/<app>, pod, container).
          discovery.relabel "pod_logs" {
            targets = discovery.kubernetes.pods.targets

            // __tmp_controller_name = controller name with the hash suffix stripped.
            rule {
              source_labels = ["__meta_kubernetes_pod_controller_name"]
              regex         = "([0-9a-z-.]+?)(-[0-9a-f]{8,10})?"
              action        = "replace"
              target_label  = "__tmp_controller_name"
            }

            // app: first non-empty of app.kubernetes.io/name, app, controller, pod name.
            rule {
              source_labels = [
                "__meta_kubernetes_pod_label_app_kubernetes_io_name",
                "__meta_kubernetes_pod_label_app",
                "__tmp_controller_name",
                "__meta_kubernetes_pod_name",
              ]
              regex        = "^;*([^;]+)(;.*)?$"
              separator    = ";"
              action       = "replace"
              target_label = "app"
            }

            // instance: first non-empty of app.kubernetes.io/instance, instance.
            rule {
              source_labels = [
                "__meta_kubernetes_pod_label_app_kubernetes_io_instance",
                "__meta_kubernetes_pod_label_instance",
              ]
              regex        = "^;*([^;]+)(;.*)?$"
              separator    = ";"
              action       = "replace"
              target_label = "instance"
            }

            // component: first non-empty of app.kubernetes.io/component, component.
            rule {
              source_labels = [
                "__meta_kubernetes_pod_label_app_kubernetes_io_component",
                "__meta_kubernetes_pod_label_component",
              ]
              regex        = "^;*([^;]+)(;.*)?$"
              separator    = ";"
              action       = "replace"
              target_label = "component"
            }

            rule {
              source_labels = ["__meta_kubernetes_pod_node_name"]
              action        = "replace"
              target_label  = "node_name"
            }

            rule {
              source_labels = ["__meta_kubernetes_namespace"]
              action        = "replace"
              target_label  = "namespace"
            }

            // job = <namespace>/<app>
            rule {
              source_labels = ["namespace", "app"]
              separator     = "/"
              action        = "replace"
              replacement   = "$1"
              target_label  = "job"
            }

            rule {
              source_labels = ["__meta_kubernetes_pod_name"]
              action        = "replace"
              target_label  = "pod"
            }

            rule {
              source_labels = ["__meta_kubernetes_pod_container_name"]
              action        = "replace"
              target_label  = "container"
            }
          }

          // Stream logs for the discovered/relabeled pods straight from the API.
          loki.source.kubernetes "pod_logs" {
            targets    = discovery.relabel.pod_logs.output
            forward_to = [loki.process.pod_logs.receiver]
          }

          // Promtail applied a `cri {}` pipeline stage; the API stream is already
          // decoded, so no CRI parsing is needed here. Forward as-is to Loki.
          loki.process "pod_logs" {
            forward_to = [loki.write.loki.receiver]
          }

          loki.write "loki" {
            endpoint {
              url = "http://loki-headless.observability.svc.cluster.local:3100/loki/api/v1/push"
            }
          }

    serviceMonitor:
      enabled: true
