---
apiVersion: v1
kind: Endpoints
metadata:
  name: vps-node-exporter
  namespace: observability
subsets:
  - addresses:
      - ip: 10.100.0.1  # VPS WireGuard IP
    ports:
      - name: metrics
        port: 9100
        protocol: TCP
