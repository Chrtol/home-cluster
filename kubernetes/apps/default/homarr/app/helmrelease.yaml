---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2beta2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homarr
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: homarr-labs
    namespace: flux-system
  postRenderers:
    - kustomize:
        patches:
          - target:
              kind: Deployment
              name: homarr
            patch: |-
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: homarr
              spec:
                template:
                  spec:
                    initContainers:
                      - name: init-db
                        image: ghcr.io/home-operations/postgres-init:18.1
                        envFrom:
                          - secretRef:
                              name: homarr-secret

  values:
    image:
      repository: ghcr.io/homarr-labs/homarr
      tag: v1.49.1@sha256:952c34da1e80ae965e738262d4dfcaf1f4e377602ab69d2d6a33f5ac3f98a8c1
      pullPolicy: IfNotPresent


    rbac:
      enabled: true
    env:
      TZ: ${SECRET_TIMEZONE}
      BASE_URL: https://start.${SECRET_DOMAIN}
      AUTH_PROVIDERS: oidc
      AUTH_OIDC_URI: https://sso.${SECRET_DOMAIN}/application/o/homarr/.well-known/openid-configuration
      AUTH_OIDC_CLIENT_NAME: authentik
      AUTH_OIDC_REDIRECT_URI: https://start.${SECRET_DOMAIN}/api/auth/callback/oidc
      AUTH_OIDC_SCOPE_OVERWRITE: "openid email profile groups"
      AUTH_OIDC_GROUPS_ATTRIBUTE: groups
      AUTH_LOGOUT_REDIRECT_URL: https://sso.${SECRET_DOMAIN}/application/o/homarr/end-session/
      AUTH_OIDC_ISSUER: https://sso.${SECRET_DOMAIN}/application/o/homarr/
      AUTH_OIDC_AUTO_LOGIN: true
      NODE_ENV: production
      NEXTAUTH_URL: https://start.${SECRET_DOMAIN}
      REDIS_IS_EXTERNAL: true
      REDIS_HOST: dragonfly.database.svc.cluster.local
      REDIS_PORT: "6379"
      REDIS_DATABASE_INDEX: "2"

    replicas: 1

    strategyType: Recreate

    livenessProbe:
      enabled: false

    readinessProbe:
      enabled: false

    lifecycle:
      preStop:
        exec:
          command: ["/bin/sh", "-c", "sleep 10"]

    ingress:
      enabled: true
      ingressClassName: external
      annotations:
        external-dns.alpha.kubernetes.io/target: "external.${SECRET_DOMAIN}"
      hosts:
        - host: &host "start.${SECRET_DOMAIN}"
          paths:
            - path: /
              pathType: ImplementationSpecific
      tls:
        - hosts:
          - *host

    podSecurityContext:
      fsGroup: 0
    securityContext:
      runAsUser: 0
      runAsGroup: 0
      allowPrivilegeEscalation: false

    httproute:
      enabled: true
      parentRefs:
          - name: internal
            namespace: network
            sectionName: https
      hostnames:
        - homarr.${SECRET_DOMAIN}
      rules:
        - matches:
            - path:
                type: PathPrefix
                value: /
          backendRefs:
            - name: homarr
              port: 7575

    database:
      type: postgresql

    # Secrets are automatically picked up by the chart with their standard names:
    # - auth-oidc-secret (OIDC authentication)
    # - db-secret (Database connection)
    # - db-encryption (Database encryption key)

    persistence:
      homarrDatabase:
        enabled: false  # Using external PostgreSQL
      homarrImages:
        enabled: true
        existingClaim: homarr
        mountPath: /images
        subPath: images
      homarrConfigs:
        enabled: true
        existingClaim: homarr
        mountPath: /app/data/configs
        subPath: configs
      homarrIcons:
        enabled: true
        existingClaim: homarr
        mountPath: /app/public/icons
        subPath: icons