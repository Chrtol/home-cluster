---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ./helmrelease.yaml
  - ./backendtrafficpolicy.yaml
  - ./externalsecret.yaml
  # Image automation for this app lives in
  # kubernetes/apps/flux-system/image-automation/app/reptile-tracker.yaml —
  # it has to share a namespace with the write credential it commits through.
  # The setter markers in helmrelease.yaml point at `flux-system:`.