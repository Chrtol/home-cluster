---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paperless
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: paperless-secret
    template:
      data:
        # App
        PAPERLESS_ADMIN_USER: "{{ .PAPERLESS_ADMIN_USER }}"
        PAPERLESS_ADMIN_PASSWORD: "{{ .PAPERLESS_ADMIN_PASSWORD }}"
        PAPERLESS_SECRET_KEY: "{{ .PAPERLESS_SECRET_KEY }}"
        # db6: moved off db5 which postiz also uses (celery + bullmq shared an
        # index by accident). Celery queue resets on the move — harmless.
        PAPERLESS_REDIS: |-
          redis://dragonfly.database.svc.cluster.local:6379/6
        # Database
        PAPERLESS_DBHOST: &dbhost "{{ .POSTGRES_RW_SVC }}"
        PAPERLESS_DBPORT: "5432"
        PAPERLESS_DBUSER: &dbuser "{{ .PAPERLESS_POSTGRES_USER }}"
        PAPERLESS_DBPASS: &dbpass "{{ .PAPERLESS_POSTGRES_PASSWORD }}"
        PAPERLESS_DBNAME: "{{ .PAPERLESS_POSTGRES_DB }}"
        # OIDC
        PAPERLESS_SOCIALACCOUNT_PROVIDERS: |-
          {
            "openid_connect": {
                "SCOPE": [
                    "openid",
                    "profile",
                    "email"
                ],
                "OAUTH_PKCE_ENABLED": true,
                "APPS": [
                    {
                        "provider_id": "authentik",
                        "name": "Authentik",
                        "client_id": "{{ .PAPERLESS_CLIENT_ID }}",
                        "secret": "{{ .PAPERLESS_CLIENT_SECRET }}",
                        "settings": {
                            "server_url": "https://sso.${SECRET_DOMAIN}/application/o/paperless/.well-known/openid-configuration"
                        }
                    }
                ]
            }
          }
  dataFrom:
    - extract:
        key: cloudnative-pg
    - extract:
        key: paperless
