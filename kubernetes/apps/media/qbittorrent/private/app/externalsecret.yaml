---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: qbittorrent-private
  namespace: media
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: qbittorrent-private-secret
    template:
      data:
        GLUETUN_CONTROL_SERVER_API_KEY: "{{ .QB_GLUETUN_API_KEY }}"
        # WireGuard (preferred — far higher throughput than OpenVPN). Generate a
        # dedicated Proton WireGuard config (NAT-PMP enabled) for THIS instance.
        WIREGUARD_PRIVATE_KEY: "{{ .PROTON_WIREGUARD_QB_PRIVATE_KEY }}"
        # OpenVPN creds kept for quick fallback if WireGuard misbehaves.
        OPENVPN_USER: "{{ .PROTON_OPENVPN_USER }}+pmp"
        OPENVPN_PASSWORD: "{{ .PROTON_OPENVPN_PASS }}"
        # XSEED_APIKEY: "{{ .CROSS_SEED_API_KEY }}"

        #Restic backup
        RESTIC_REPOSITORY: "/repository/qbittorrent-private"
        RESTIC_PASSWORD: "{{ .RESTIC_PASSWORD }}"
  dataFrom:
    - extract:
        key: qbittorrent
    - extract:
        key: protonvpn
    - extract:
        key: restic
    # - extract:
    #     key: cross-seed
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: qbittorrent-private-gluetun
  namespace: media
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: qbittorrent-private-gluetun-secret
    template:
      data:
        auth.toml: |
          [[roles]]
          name = "gluetun-qb-port-sync"
          routes = [
            "GET /v1/publicip/ip",
            "GET /v1/openvpn/portforwarded",
            "GET /v1/portforward",
            # port-watchdog: on a sustained forwarded-port==0 (gluetun PF loop
            # stuck-at-0 bug, fixed only on master/latest — we stay on v3.41.1),
            # cycle the tunnel to force a new server + fresh NAT-PMP port without
            # restarting the pod (which would tear down qBittorrent's netns).
            "GET /v1/vpn/status",
            "PUT /v1/vpn/status"
          ]
          auth = "apikey"
          apikey = "{{ .QB_GLUETUN_API_KEY }}"
  dataFrom:
    - extract:
        key: qbittorrent