---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/k8s.mariadb.com/grant_v1alpha1.json
apiVersion: k8s.mariadb.com/v1alpha1
kind: Grant
metadata:
  name: booklore
spec:
  mariaDbRef:
    name: mariadb-operator
  privileges:
    - ALL PRIVILEGES
  database: booklore
  table: "*"
  username: booklore
  host: "%"
  requeueInterval: 30s
  retryInterval: 5s
