---
# This Job creates the Garage bucket and access key for Outline
# and stores the credentials in a Kubernetes secret
apiVersion: batch/v1
kind: Job
metadata:
  name: garage-outline-bucket-setup
  namespace: database
spec:
  # Clean up after success
  ttlSecondsAfterFinished: 300  # 5 minutes
  backoffLimit: 3
  template:
    spec:
      serviceAccountName: garage-bucket-creator
      restartPolicy: OnFailure
      containers:
        - name: setup
          image: dxflrs/garage:v1.3.0
          command:
            - /garage
            - --version
          env:
            - name: GARAGE_RPC_SECRET
              valueFrom:
                secretKeyRef:
                  name: garage-secret
                  key: SECRET_GARAGE_RPC_SECRET
          volumeMounts:
            - name: config
              mountPath: /etc/garage.toml
              subPath: config.toml
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: config.toml