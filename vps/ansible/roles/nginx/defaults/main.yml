---
# Nginx configuration defaults
# These are overridden by environment variables from 1Password

# Nginx performance settings
nginx_worker_processes: auto
nginx_worker_connections: 2048
nginx_client_max_body_size: 100M

# Feature toggles (can be overridden in playbook)
enable_echo_test: true
enable_metrics: true