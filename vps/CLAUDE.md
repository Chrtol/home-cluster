# VPS (Ansible)

Ansible configuration for the VPS, deployed via CI/CD.

- Must be declarative and idempotent
- No hardcoded secrets, domains, or IP addresses
- All secrets/config from 1Password integration
