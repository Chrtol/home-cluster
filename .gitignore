# Secrets
*.pub
*.key
*.decrypted~*.yaml
/age.key
/cloudflare-tunnel.json
/github-deploy.key
/github-deploy.key.pub
/github-push-token.txt
# Template config files
/cluster.yaml
/nodes.yaml
# Kubernetes
kubeconfig
talosconfig
# Misc.
.private/
.task/
.venv/
.DS_Store
Thumbs.db
.sopsrc
1password-credentials*
ai-activity/*
# Log files
*.log
# Planning lives in the private home-planning repo, and each app now carries
# its own in its own private repo. This guards the PUBLIC repo against a
# stray GSD Core run writing planning content here.
.planning
tmp/*
.env
.kube/
