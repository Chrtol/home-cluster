---
phase: 08-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dashboard/ReptileStatusCard.jsx
  - frontend/src/components/dashboard/ReptileStatusCards.jsx
  - frontend/src/components/dashboard/TaskChip.jsx
  - frontend/src/utils/displaySettings.js
  - frontend/src/pages/Dashboard.jsx
autonomous: true

must_haves:
  truths:
    - "User sees all reptiles as status cards on dashboard"
    - "Cards show photo, name, species, age, last fed, weight trend"
    - "Cards show today's tasks as clickable chips"
    - "Cards auto-switch to compact mode when 6+ reptiles"
    - "User can drag cards to reorder directly on dashboard"
    - "Clicking card body expands inline, clicking name navigates to detail"
    - "Status indicator dot shows green/amber/red based on task status"
    - "Cards with overdue tasks have amber border"
  artifacts:
    - path: "frontend/src/components/dashboard/ReptileStatusCard.jsx"
      provides: "Individual reptile status card component"
      exports: ["ReptileStatusCard"]
    - path: "frontend/src/components/dashboard/ReptileStatusCards.jsx"
      provides: "Container with grid layout, compact mode, drag-drop"
      exports: ["ReptileStatusCards"]
    - path: "frontend/src/components/dashboard/TaskChip.jsx"
      provides: "Clickable task chip with status styling"
      exports: ["TaskChip"]
    - path: "frontend/src/utils/displaySettings.js"
      provides: "reptile_status_cards widget definition"
      contains: "reptile_status_cards"
  key_links:
    - from: "frontend/src/components/dashboard/ReptileStatusCards.jsx"
      to: "/api/reptiles"
      via: "axios fetch with today's tasks"
      pattern: "axios\\.get.*reptiles"
    - from: "frontend/src/pages/Dashboard.jsx"
      to: "frontend/src/components/dashboard/ReptileStatusCards.jsx"
      via: "widget card rendering"
      pattern: "ReptileStatusCards"
---

<objective>
Create the Reptile Status Cards widget - the hero section of the new dashboard showing each reptile's status at a glance.

Purpose: Give users immediate visibility into all reptiles' current state without scrolling, with quick actions for logging tasks.

Output: ReptileStatusCards widget integrated into dashboard with full/compact modes, drag reorder, and task chips.
</objective>

<execution_context>
@/home/chrto/.claude/get-shit-done/workflows/execute-plan.md
@/home/chrto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/mockups/dashboard-v1.1-concept.html
@.planning/phases/08-dashboard/08-CONTEXT.md
@.planning/phases/08-dashboard/08-RESEARCH.md
@frontend/src/pages/Dashboard.jsx
@frontend/src/utils/displaySettings.js
@frontend/src/components/ReptileNameWithAvatar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskChip Component</name>
  <files>frontend/src/components/dashboard/TaskChip.jsx</files>
  <action>
Create a reusable TaskChip component for displaying task status:

1. Create directory: `frontend/src/components/dashboard/`

2. Create TaskChip.jsx with these features:
   - Props: task, onClick, className
   - Status styles per user decision:
     - done: bg-status-done/20 text-status-done with checkmark
     - due: bg-surface-600 text-gray-400
     - overdue: bg-status-overdue/20 text-status-overdue
   - Display: task name + time if present (e.g., "misting 20:00")
   - Click handler: calls onClick to trigger quick-log (implementation in later plan)
   - Accessible: role="button", keyboard support (Enter/Space)
   - Size: text-xs, px-1.5 py-0.5, rounded

3. Style matching mockup:
```jsx
const statusStyles = {
  done: 'bg-status-done/20 text-status-done',
  due: 'bg-surface-600 text-gray-400',
  overdue: 'bg-status-overdue/20 text-status-overdue'
};
```

4. Format display:
   - Done tasks: "checkmark salad"
   - Due tasks: "food 20:00"
   - Overdue: "food overdue"
  </action>
  <verify>
- TaskChip renders with correct styling for each status
- Click handler fires on click and Enter/Space
- Text formatting matches mockup
- Run: `npm run build` succeeds
  </verify>
  <done>TaskChip component created with status-based styling and click handler</done>
</task>

<task type="auto">
  <name>Task 2: Create ReptileStatusCard Component</name>
  <files>frontend/src/components/dashboard/ReptileStatusCard.jsx</files>
  <action>
Create the individual reptile status card matching mockup exactly:

1. Props:
   - reptile: Reptile object with name, species, avatar_photo_url, birth_date, etc.
   - todayTasks: Array of today's schedule instances for this reptile
   - lastFed: Date of last feeding (or null)
   - lastWeight: {weight, change} object (or null)
   - isCompact: Boolean for compact mode display
   - onReorder: Drag handlers for reordering
   - onQuickLog: Handler for task chip clicks
   - index: Position for drag-drop

2. Full card layout (isCompact=false):
   - Large avatar (w-16 h-16) with status ring color
   - Name (font-semibold, links to /reptiles/:id on click)
   - Age display (calculated from birth_date/hatch_date)
   - Species (text-xs text-gray-400)
   - Quick stats row: last fed (Xd ago), weight with trend arrow
   - Today's tasks section (border-t) with TaskChip components
   - Show max 3 chips + "+X more" if >3 tasks per user decision

3. Compact card layout (isCompact=true):
   - Smaller layout: avatar (w-12 h-12) + name + status dot only
   - Expand on hover/click to show full details inline
   - Use inline expand approach (per Claude's discretion - simpler, matches mockup feel)

4. Status indicator:
   - Colored ring around avatar based on task status
   - Green (accent-600): all tasks done or no tasks
   - Amber (status-due): has due tasks
   - Red (status-overdue): has overdue tasks
   - Small status dot at bottom-right of avatar

5. Border styling per user decision:
   - Overdue: border-status-due (amber)
   - Default: border-surface-600/50
   - Hover: border-accent-700/50

6. Click behavior per user decision:
   - Card body click: expand inline (compact mode) or no-op (full mode)
   - Name/avatar click: navigate to /reptiles/:id

7. Drag-drop support:
   - draggable attribute
   - onDragStart, onDragOver, onDrop handlers
   - Visual feedback (opacity-50 while dragging)
   - Hidden on mobile (use touch-action: none)

8. No photo fallback per user decision:
   - If no avatar_photo_url, show species emoji/icon
   - Map species to emoji (gecko, dragon, snake -> specific emojis)
  </action>
  <verify>
- Card displays all required info (photo, name, species, age, stats)
- Status ring color changes based on task status
- Task chips render with correct styling
- "+X more" appears when >3 tasks
- Drag handle works on desktop
- Compact mode shows reduced layout
- Click name navigates to reptile detail page
- Run: `npm run build` succeeds
  </verify>
  <done>ReptileStatusCard component created with full and compact modes, status indicators, drag support</done>
</task>

<task type="auto">
  <name>Task 3: Create ReptileStatusCards Container and Integrate Widget</name>
  <files>frontend/src/components/dashboard/ReptileStatusCards.jsx, frontend/src/utils/displaySettings.js, frontend/src/pages/Dashboard.jsx</files>
  <action>
Create the container component and integrate as a dashboard widget:

1. Create ReptileStatusCards.jsx:
   - Props: config (from widget settings), size (from widget size)
   - Fetch reptiles with today's schedules, last feeding, last weight data
   - Use existing Dashboard.jsx fetch patterns as reference
   - Manage local state for reptile order (drag reordering)

2. Auto-compact mode per user decision:
   - threshold = config?.compactThreshold || 6
   - Auto-enable compact when reptiles.length >= threshold
   - Store user override in localStorage if they manually toggle

3. Drag-to-reorder:
   - Track draggedIndex state
   - On drop: reorder reptiles array
   - Persist order to localStorage: `reptile_card_order`
   - Key: array of reptile IDs in user's preferred order

4. Grid layout based on mode and widget size:
```jsx
const getGridClasses = (size, isCompact) => {
  if (isCompact) {
    return 'grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2';
  }
  const sizeMap = {
    large: 'grid-cols-1 lg:grid-cols-2 gap-3',
    medium: 'grid-cols-1 lg:grid-cols-2 gap-3',
    small: 'grid-cols-1 gap-3'
  };
  return sizeMap[size] || sizeMap.large;
};
```

5. Update displaySettings.js:
   - Add reptile_status_cards to DEFAULT_DASHBOARD_CARDS:
```js
{
  id: 'reptile_status_cards',
  label: 'Reptile Status Cards',
  visible: true,
  order: 0,  // First position
  size: 'large',
  type: 'content',
  config: {
    compactThreshold: 6,
    showAge: true,
    showWeight: true
  }
}
```
   - Update all three default profiles (Standard, Compact, Mobile)
   - Update migration logic to add this card to existing user profiles

6. Integrate into Dashboard.jsx:
   - Import ReptileStatusCards
   - Add case in widget rendering switch:
```jsx
case 'reptile_status_cards':
  return <ReptileStatusCards key={card.id} config={card.config} size={card.size} />;
```
   - Ensure widget respects visibility and order from displaySettings

7. Handle quick-log clicks (prepare for Plan 08-03):
   - onQuickLog prop that will be wired to quick-log form later
   - For now, console.log the task ID or show a toast placeholder
  </action>
  <verify>
- Dashboard shows ReptileStatusCards widget
- Cards display in grid layout
- Auto-compact triggers at 6+ reptiles
- Drag-to-reorder works and persists
- Widget respects displaySettings visibility/order
- Existing dashboard cards still work
- Profile switching includes new widget
- Run: `npm run build` succeeds
  </verify>
  <done>ReptileStatusCards widget integrated into dashboard with auto-compact, drag reorder, and widget system compatibility</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Dashboard shows reptile status cards
2. Each card shows: photo, name, species, age, last fed, weight trend
3. Today's tasks appear as clickable chips with status colors
4. Cards with overdue tasks have amber border
5. Auto-compact mode triggers at 6+ reptiles
6. Compact cards expand inline on click
7. Drag-to-reorder works (desktop) and persists
8. Clicking name navigates to reptile detail
9. Widget integrates with existing displaySettings system
10. All three default profiles include the new widget
11. `npm run build` passes
</verification>

<success_criteria>
- Reptile status visible at a glance
- Task status immediately apparent (done/due/overdue colors)
- Compact mode handles many reptiles gracefully
- Drag reorder works without external library
- Widget system compatibility maintained
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard/08-02-SUMMARY.md`
</output>
