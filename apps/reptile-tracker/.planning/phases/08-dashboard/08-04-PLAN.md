---
phase: 08-dashboard
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/components/dashboard/WeightTrendsWidget.jsx
  - frontend/src/components/dashboard/WeekSummaryWidget.jsx
  - frontend/src/components/dashboard/RecentActivityWidget.jsx
  - frontend/src/components/dashboard/WidgetGallery.jsx
  - frontend/src/components/dashboard/EditModeControls.jsx
  - frontend/src/utils/displaySettings.js
  - frontend/src/pages/Dashboard.jsx
autonomous: true

must_haves:
  truths:
    - "User sees weight trends with sparkline charts"
    - "User sees this week summary stats (feedings, mistings, scheduled, overdue)"
    - "User sees compact recent activity list"
    - "User can enter edit mode to customize widgets"
    - "Edit mode shows widget gallery to add new widgets"
    - "Edit mode allows reset to default layout"
    - "Widget gallery shows visual previews"
    - "Per-widget settings accessible in edit mode"
    - "Minimal animations on add/remove/reorder"
  artifacts:
    - path: "frontend/src/components/dashboard/WeightTrendsWidget.jsx"
      provides: "Weight trends with sparklines"
      exports: ["WeightTrendsWidget"]
    - path: "frontend/src/components/dashboard/WeekSummaryWidget.jsx"
      provides: "Weekly summary statistics"
      exports: ["WeekSummaryWidget"]
    - path: "frontend/src/components/dashboard/RecentActivityWidget.jsx"
      provides: "Compact recent activity list"
      exports: ["RecentActivityWidget"]
    - path: "frontend/src/components/dashboard/WidgetGallery.jsx"
      provides: "Gallery for adding widgets"
      exports: ["WidgetGallery"]
    - path: "frontend/src/components/dashboard/EditModeControls.jsx"
      provides: "Edit mode toggle and controls"
      exports: ["EditModeControls"]
  key_links:
    - from: "frontend/src/components/dashboard/WeightTrendsWidget.jsx"
      to: "recharts LineChart"
      via: "sparkline rendering"
      pattern: "LineChart|ResponsiveContainer"
    - from: "frontend/src/components/dashboard/EditModeControls.jsx"
      to: "frontend/src/utils/displaySettings.js"
      via: "reset and save operations"
      pattern: "resetDashboardCardSettings|saveDashboardCardSettings"
---

<objective>
Complete the dashboard widgets: weight trends sparklines, week summary stats, compact recent activity, and the edit mode system for widget customization.

Purpose: Round out the "single pane of glass" dashboard with trend visualization and activity history, while enabling full widget customization per user preferences.

Output: Three new content widgets plus edit mode controls and widget gallery for customization.
</objective>

<execution_context>
@/home/chrto/.claude/get-shit-done/workflows/execute-plan.md
@/home/chrto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/mockups/dashboard-v1.1-concept.html
@.planning/phases/08-dashboard/08-CONTEXT.md
@.planning/phases/08-dashboard/08-RESEARCH.md
@frontend/src/pages/Dashboard.jsx
@frontend/src/utils/displaySettings.js
@frontend/src/components/ProfileManager.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Weight Trends and Week Summary Widgets</name>
  <files>frontend/src/components/dashboard/WeightTrendsWidget.jsx, frontend/src/components/dashboard/WeekSummaryWidget.jsx</files>
  <action>
Create two statistics widgets matching mockup:

**WeightTrendsWidget.jsx:**
1. Props: config, size
2. Fetch weight data from API (last 90 days, or config.timeRange)
3. Display per reptile:
   - Avatar (w-6 h-6)
   - Name
   - Current weight
   - Sparkline chart (Recharts LineChart, minimal config)
   - Change percentage (since last measurement per user decision)

4. Sparkline implementation:
```jsx
import { LineChart, Line, ResponsiveContainer } from 'recharts';

// Minimal sparkline - no axes, no grid, no legend
<ResponsiveContainer width="100%" height={16}>
  <LineChart data={reptile.weightData}>
    <Line
      type="monotone"
      dataKey="weight"
      stroke="#22c55e"
      strokeWidth={2}
      dot={false}
    />
  </LineChart>
</ResponsiveContainer>
```

5. Time range selector:
   - Dropdown: 30 days, 90 days (default), 180 days
   - Store in config.timeRange

6. Change color:
   - Positive: text-accent-400 (green)
   - Negative: text-status-due (amber)
   - Format: "+29%" or "-5%"

7. Only show reptiles with weight data (skip those with no measurements)

**WeekSummaryWidget.jsx:**
1. Props: config, size
2. Fetch weekly aggregated data from API
3. Display 4 stat cards per mockup:
   - Feedings count (accent-400 green)
   - Mistings count (status-mist blue)
   - Scheduled count (white)
   - Overdue count (status-overdue red)

4. Layout:
   - 2x2 grid of stat cards
   - Each card: bg-surface-700/50 rounded-lg p-2
   - Large number (text-lg font-semibold)
   - Label below (text-[10px] text-gray-500)

5. Week range in header:
   - Calculate current week dates
   - Display: "Feb 3 - 9" format
  </action>
  <verify>
- WeightTrendsWidget shows sparklines for reptiles with weight data
- Sparklines render correctly with Recharts
- Change percentage calculated from last measurement
- Time range dropdown works
- WeekSummaryWidget shows 4 stat cards
- Week dates displayed correctly
- Colors match mockup
- Run: `npm run build` succeeds
  </verify>
  <done>Weight trends with sparklines and week summary stats widgets created</done>
</task>

<task type="auto">
  <name>Task 2: Create Recent Activity Widget and Update displaySettings</name>
  <files>frontend/src/components/dashboard/RecentActivityWidget.jsx, frontend/src/utils/displaySettings.js</files>
  <action>
Create compact recent activity and add all new widgets to displaySettings:

**RecentActivityWidget.jsx:**
1. Props: config, size
2. Reuse existing recentActivity data pattern from Dashboard.jsx
3. Compact layout per mockup:
   - Avatar (w-6 h-6)
   - Activity description (truncated, text-xs)
   - Reptile name + time ago (text-[10px] text-gray-500)
   - Quantity if applicable (text-xs text-gray-500)
   - "View all" link to activity page

4. Limit display:
   - Show 3-5 items based on widget size
   - xs/small: 3 items
   - medium/large: 5 items

5. Hover state: bg-surface-700/50 for row highlight

**Update displaySettings.js:**
1. Add new widget definitions to DEFAULT_DASHBOARD_CARDS:
```js
{
  id: 'weight_trends',
  label: 'Weight Trends',
  visible: true,
  order: 5,
  size: 'small',
  type: 'content',
  config: {
    timeRange: 90
  }
},
{
  id: 'week_summary',
  label: 'This Week',
  visible: true,
  order: 6,
  size: 'small',
  type: 'content'
},
{
  id: 'compact_recent_activity',
  label: 'Recent Activity',
  visible: true,
  order: 7,
  size: 'small',
  type: 'content',
  config: {
    itemCount: 5
  }
}
```

2. Update all three default profiles with appropriate sizing:
   - Standard: All visible, per mockup layout
   - Compact: Smaller sizes
   - Mobile: Full width

3. Migration: Add new widgets to existing user profiles

4. Consider replacing old 'recent_activity' with 'compact_recent_activity':
   - Or keep both for backward compatibility
   - Mark old one as hidden by default if superseded
  </action>
  <verify>
- RecentActivityWidget shows compact activity list
- Item count respects widget size
- View all link works
- New widgets appear in DEFAULT_DASHBOARD_CARDS
- Profiles updated with new widgets
- Migration adds widgets to existing profiles
- Run: `npm run build` succeeds
  </verify>
  <done>Recent activity widget and displaySettings updates complete with all new widgets</done>
</task>

<task type="auto">
  <name>Task 3: Create Edit Mode Controls and Widget Gallery</name>
  <files>frontend/src/components/dashboard/EditModeControls.jsx, frontend/src/components/dashboard/WidgetGallery.jsx, frontend/src/pages/Dashboard.jsx</files>
  <action>
Create edit mode system for widget customization:

**EditModeControls.jsx:**
1. Props: isEditMode, onToggleEditMode, onResetLayout
2. Toggle button in dashboard header:
   - When not editing: "Customize" button
   - When editing: "Done" button + "Reset" button

3. Edit mode visual changes:
   - Widgets get subtle border/glow to indicate editability
   - Drag handles visible on widget headers
   - Settings icon appears for per-widget config

4. Reset layout:
   - Confirm dialog before reset
   - Use resetDashboardCardSettings() from displaySettings
   - Reload page or refresh state after reset

**WidgetGallery.jsx:**
1. Props: availableWidgets, onAddWidget, onClose
2. Modal/dropdown showing available widgets:
   - Grid of widget preview cards
   - Each shows: icon, name, brief description, preview thumbnail
   - Click to add widget (sets visible: true)

3. Available widgets list:
   - Filter out widgets already visible
   - Show disabled state for already-added widgets

4. Visual preview:
   - Simplified representation of widget content
   - Could be static image or mini rendering

5. Animation on add:
   - Use Framer Motion AnimatePresence
   - Subtle fade-in and scale (per user decision: minimal animations)

**Dashboard.jsx updates:**
1. Add isEditMode state
2. Render EditModeControls in header
3. Pass edit mode to widget containers for visual changes
4. Show "+ Add Widget" button in edit mode
5. Clicking "+ Add Widget" opens WidgetGallery

6. Per-widget settings in edit mode:
   - Settings gear icon on each widget header
   - Opens small config modal/popover
   - Fields depend on widget type (e.g., timeRange for trends)
   - Save updates widget config in displaySettings

7. Widget animations with Framer Motion:
```jsx
import { motion, AnimatePresence } from 'framer-motion';

<AnimatePresence mode="popLayout">
  {visibleWidgets.map(widget => (
    <motion.div
      key={widget.id}
      layout
      initial={{ opacity: 0, scale: 0.95 }}
      animate={{ opacity: 1, scale: 1 }}
      exit={{ opacity: 0, scale: 0.95 }}
      transition={{ duration: 0.2 }}
    >
      {renderWidget(widget)}
    </motion.div>
  ))}
</AnimatePresence>
```

8. Widget reordering in edit mode:
   - Drag handles visible
   - Use existing drag-drop pattern from displaySettings
   - Save order on drop
  </action>
  <verify>
- Edit mode toggle works
- Widgets show visual indicators when in edit mode
- Widget Gallery opens and shows available widgets
- Adding a widget makes it visible on dashboard
- Reset layout restores defaults
- Per-widget settings accessible and persist
- Add/remove animations are smooth but minimal
- Drag reorder works in edit mode
- Run: `npm run build` succeeds
  </verify>
  <done>Edit mode controls and widget gallery created with full customization capability</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Weight trends widget shows sparklines for each reptile
2. Change percentage shows trend since last measurement
3. Week summary shows 4 stat cards with correct counts
4. Recent activity widget shows compact list
5. Edit mode toggle visible in dashboard header
6. Edit mode shows visual indicators on widgets
7. Widget Gallery shows available widgets with previews
8. Can add hidden widgets from gallery
9. Reset layout restores default configuration
10. Per-widget settings work (e.g., timeRange)
11. Add/remove widgets have subtle animations
12. All widgets integrate with displaySettings system
13. `npm run build` passes
</verification>

<success_criteria>
- Complete "single pane of glass" dashboard with all widgets
- Users can fully customize their dashboard layout
- Widget gallery provides discoverability
- Settings persist across sessions
- Clean, minimal animations
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard/08-04-SUMMARY.md`
</output>
