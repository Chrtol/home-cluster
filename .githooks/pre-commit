#!/usr/bin/env bash
# Block commits that carry a plaintext cluster secret into this public repo.
# Enable with: git config core.hooksPath .githooks
set -euo pipefail
exec "$(git rev-parse --show-toplevel)/scripts/scan-secret-leaks.sh" staged
