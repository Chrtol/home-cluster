{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  extends: [
    'config:recommended',
    'docker:enableMajor',
    'helpers:pinGitHubActionDigests',
    ':automergeBranch',
    ':dependencyDashboard',
    ':disableRateLimiting',
    ':semanticCommits',
  ],
  dependencyDashboard: true,
  dependencyDashboardTitle: 'Renovate Dashboard 🤖',
  suppressNotifications: [
    'prEditedNotification',
    'prIgnoreNotification',
  ],
  schedule: [
    'every weekend',
  ],
  ignorePaths: [
    '**/*.sops.*',
    'apps/**',
  ],
  // ghcr.io requires a bearer token even for public images when resolving manifest
  // digests; anonymous lookups get rate-limited, producing "No docker auth found"
  // + "Could not determine new digest" for ghcr.io images (affine-graphql, cnpg).
  // Token: a classic GitHub PAT with ONLY `read:packages`, stored PLAIN-TEXT as a
  // secret named GHCR_TOKEN in the Mend dashboard (developer.mend.io -> Credentials
  // -> ADD SECRET). Mend no longer supports encrypted blobs in the config file.
  hostRules: [
    {
      matchHost: 'ghcr.io',
      token: '{{ secrets.GHCR_TOKEN }}',
    },
  ],
  flux: {
    managerFilePatterns: [
      '/(^|/)kubernetes/.+\\.ya?ml(?:\\.j2)?$/',
    ],
  },
  'helm-values': {
    managerFilePatterns: [
      '/(^|/)kubernetes/.+\\.ya?ml(?:\\.j2)?$/',
    ],
  },
  helmfile: {
    managerFilePatterns: [
      '/(^|/)helmfile\\.ya?ml(?:\\.j2)?$/',
    ],
  },
  kubernetes: {
    managerFilePatterns: [
      '/(^|/)kubernetes/.+\\.ya?ml(?:\\.j2)?$/',
    ],
  },
  kustomize: {
    managerFilePatterns: [
      '/(^|/)kustomization\\.ya?ml(?:\\.j2)?$/',
    ],
  },
  packageRules: [
    {
      // gateway-api experimental channel must serve TLSRoute under BOTH v1 (Envoy
      // Gateway 1.8.x) and v1alpha2 (Cilium 1.19.x operator field indexer). The
      // experimental channel through v1.6.0 serves v1 + v1alpha2 + v1alpha3, so both
      // controllers are satisfied. Cap to <1.7.0 as a safety margin: a future release
      // could drop v1alpha2 (breaking Cilium) — re-verify TLSRoute served versions
      // before lifting. The safe-upgrades VAP is excluded in crds.yaml.
      description: 'Cap gateway-api CRDs to <1.7.0; experimental must serve TLSRoute v1 + v1alpha2',
      matchDatasources: [
        'github-releases',
      ],
      matchPackageNames: [
        'kubernetes-sigs/gateway-api',
      ],
      allowedVersions: '<1.7.0',
    },
    {
      // v0.26.1 entrypoint requires a writable /home and root to adduser/addgroup,
      // incompatible with our runAsNonRoot + readOnlyRootFilesystem securityContext
      // (crashloops). Cap to <0.26.0 until the manifest adds a writable /home tmpfs.
      description: 'Cap paperless-gpt to <0.26.0 (needs writable /home under hardened securityContext)',
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        'ghcr.io/icereed/paperless-gpt',
      ],
      allowedVersions: '<0.26.0',
    },
    {
      // The app image (ghcr.io/kanbn/kan) and its migration image
      // (ghcr.io/kanbn/kan-migrate) are built from the same source repo and MUST
      // move in lockstep — a version skew reintroduces schema drift (app newer than
      // migrate = 500s on missing columns). Group them into a single PR so they can
      // never bump independently.
      description: 'Kan Group (app + migrate images in lockstep)',
      groupName: 'Kan',
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        'ghcr.io/kanbn/kan',
        'ghcr.io/kanbn/kan-migrate',
      ],
      group: {
        commitMessageTopic: '{{{groupName}}} group',
      },
    },
    {
      description: 'Cert-Manager Group',
      groupName: 'Cert-Manager',
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        '/cert-manager/',
      ],
      group: {
        commitMessageTopic: '{{{groupName}}} group',
      },
    },
    {
      description: 'CoreDNS Group',
      groupName: 'CoreDNS',
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        '/coredns/',
      ],
      group: {
        commitMessageTopic: '{{{groupName}}} group',
      },
    },
    {
      description: 'Flux Operator Group',
      groupName: 'Flux Operator',
      matchPackageNames: [
        '/flux-operator/',
        '/flux-instance/',
      ],
      matchDatasources: [
        'docker',
      ],
      group: {
        commitMessageTopic: '{{{groupName}}} group',
      },
    },
    {
      description: 'Spegel Group',
      groupName: 'Spegel',
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        '/spegel/',
      ],
      group: {
        commitMessageTopic: '{{{groupName}}} group',
      },
    },
    {
      // The operator ships as an OCI chart AND a separate image, and the HelmRelease
      // pins image.tag explicitly (must match the chart). Group both so they can never
      // bump independently -- a chart-only bump would silently leave the old image.
      description: 'Garage Operator Group (chart + image in lockstep)',
      groupName: 'Garage Operator',
      matchDatasources: [
        'docker',
      ],
      matchPackageNames: [
        '/rajsinghtech(/charts)?/garage-operator/',
      ],
      group: {
        commitMessageTopic: '{{{groupName}}} group',
      },
    },
    {
      description: 'Auto-merge GitHub Actions',
      matchManagers: [
        'github-actions',
      ],
      automerge: true,
      automergeType: 'branch',
      matchUpdateTypes: [
        'minor',
        'patch',
        'digest',
      ],
      minimumReleaseAge: '3 days',
      ignoreTests: true,
    },
    {
      matchUpdateTypes: [
        'major',
      ],
      semanticCommitType: 'feat',
      commitMessagePrefix: '{{semanticCommitType}}({{semanticCommitScope}})!:',
      commitMessageExtra: '( {{currentVersion}} → {{newVersion}} )',
    },
    {
      matchUpdateTypes: [
        'minor',
      ],
      semanticCommitType: 'feat',
      commitMessageExtra: '( {{currentVersion}} → {{newVersion}} )',
    },
    {
      matchUpdateTypes: [
        'patch',
      ],
      semanticCommitType: 'fix',
      commitMessageExtra: '( {{currentVersion}} → {{newVersion}} )',
    },
    {
      matchUpdateTypes: [
        'digest',
      ],
      semanticCommitType: 'chore',
      commitMessageExtra: '( {{currentDigestShort}} → {{newDigestShort}} )',
    },
    {
      matchDatasources: [
        'docker',
      ],
      semanticCommitScope: 'container',
      commitMessageTopic: 'image {{depName}}',
    },
    {
      matchDatasources: [
        'helm',
      ],
      semanticCommitScope: 'helm',
      commitMessageTopic: 'chart {{depName}}',
    },
    {
      matchManagers: [
        'github-actions',
      ],
      semanticCommitType: 'ci',
      semanticCommitScope: 'github-action',
      commitMessageTopic: 'action {{depName}}',
    },
    {
      matchDatasources: [
        'github-releases',
      ],
      semanticCommitScope: 'github-release',
      commitMessageTopic: 'release {{depName}}',
    },
    {
      matchUpdateTypes: [
        'major',
      ],
      labels: [
        'type/major',
      ],
    },
    {
      matchUpdateTypes: [
        'minor',
      ],
      labels: [
        'type/minor',
      ],
    },
    {
      matchUpdateTypes: [
        'patch',
      ],
      labels: [
        'type/patch',
      ],
    },
    {
      matchDatasources: [
        'docker',
      ],
      addLabels: [
        'renovate/container',
      ],
    },
    {
      matchDatasources: [
        'helm',
      ],
      addLabels: [
        'renovate/helm',
      ],
    },
    {
      matchManagers: [
        'github-actions',
      ],
      addLabels: [
        'renovate/github-action',
      ],
    },
    {
      matchDatasources: [
        'github-releases',
      ],
      addLabels: [
        'renovate/github-release',
      ],
    },
  ],
  customManagers: [
    {
      description: 'Process annotated dependencies',
      customType: 'regex',
      managerFilePatterns: [
        '/(^|/).+\\.env(?:\\.j2)?$/',
        '/(^|/).+\\.sh(?:\\.j2)?$/',
        '/(^|/).+\\.ya?ml(?:\\.j2)?$/',
      ],
      matchStrings: [
        // Split an optional @sha256 digest out of the value so docker versioning can
        // parse the tag (a combined "tag@sha256:..." currentValue is silently skipped).
        'datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)( repository=(?<registryUrl>\\S+))?\\n.+(:\\s|=)(&\\S+\\s)?(?<currentValue>[^@\\s]+)(@(?<currentDigest>sha256:[a-f0-9]+))?',
        'datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\n.+/(?<currentValue>(v|\\d)[^/]+)',
      ],
      datasourceTemplate: '{{#if datasource}}{{{datasource}}}{{else}}github-releases{{/if}}',
    },
  ],
}
